/* the usage of enums goes like: 
 * ENUM.WAIT.value = WAIT
 * ENUM.values = ['WAIT','ACCEPTED', 'DELIVERED', 'REFUSED' ]
 * ENUM.index = 0,1,...
*/

enum DONATION_STATUS { WAIT, ACCEPTED, DELIVERED, REFUSED }

const URL = '';
